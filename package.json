{
  "name": "droploop",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@9.15.9",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "typecheck": "turbo typecheck",
    "lint": "turbo lint",
    "test": "turbo test",
    "test:unit": "vitest run",
    "preview:loop-doctor": "pnpm --filter @droploop/worker prepare:loop-doctor-preview && pnpm --filter @droploop/web dev",
    "verify": "pnpm typecheck && pnpm test:unit && pnpm build"
  },
  "devDependencies": {
    "@types/node": "^20.17.10",
    "turbo": "^2.3.3",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
