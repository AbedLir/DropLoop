{
  "name": "@droploop/database",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "test": "tsc --noEmit",
    "test:integration": "tsx src/verify-control-plane.ts",
    "migrate": "tsx src/migrate-cli.ts"
  },
  "dependencies": {
    "@droploop/pipeline": "workspace:*",
    "@droploop/schemas": "workspace:*",
    "postgres": "3.4.9"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
